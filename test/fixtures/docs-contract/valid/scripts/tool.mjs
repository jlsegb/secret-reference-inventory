/**
 * Returns the supplied script fixture value.
 *
 * Inputs: A string value.
 * Outputs: The supplied value.
 * Does not handle: Value normalization.
 * Side effects: None.
 */
export function run(value) {
  return value;
}
